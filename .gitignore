# 构建输出 / Build output
build/
*.o
*.obj
*.exe

# 测试 ROM（版权归原作者，建议各自本地保存）/ Test ROMs (respect upstream licenses)
roms/*.ch8
roms/*.sc8
roms/**/*.ch8
roms/*.gb
roms/**/*.gb

# 第三方二进制库 / Vendored binaries (re-download locally; don't commit)
*.dll
*.a
chip8/SDL3/

# 进化/记忆系统（本地私人记录，含绝对路径，不入库）/ Evolution memory
.evolve/

# IDE / 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# 调试输出 / Debug trace output
trace*.txt
*.log
